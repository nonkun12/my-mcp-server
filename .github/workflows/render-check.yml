name: Render Deploy Check

on:
  push:
    branches:
      - main

jobs:
  check-render:
    runs-on: ubuntu-latest

    steps:
      - name: Wait Render Deploy
        run: sleep 60

      - name: Check MCP Health
        run: |
          curl -f https://my-mcp-server-dqbx.onrender.com/health
