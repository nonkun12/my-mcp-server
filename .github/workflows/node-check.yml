name: Node Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Syntax check
        run: |
          node --check index.js
          node --check database.js
          node --check delete-reminder.js
          node --check agent-dispatch-contract.js
          node --check agent-dispatch-queue.js
          find tools -type f -name '*.js' -print0 | xargs -0 -r -n1 node --check
      - name: Dispatch contract tests
        run: node --test agent-dispatch-contract.test.js agent-dispatch-queue.test.js
